buildscript {
	repositories {
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
	}
	dependencies {
		classpath "net.fabricmc:fabric-loom:${loom_version}"
	}
}

apply plugin: 'fabric-loom'
apply plugin: 'maven-publish'

ext.bandSuffix = 'mc121'
ext.minecraftVersion = project.minecraft_version_121
ext.yarnMappings = project.yarn_mappings_121
ext.fabricVersion = project.fabric_version_121
ext.minecraftDepRange = project.minecraft_dep_range_121

apply from: rootProject.file('gradle/mc-band.gradle')
